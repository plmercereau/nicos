---
title: "Machine options"
sidebarTitle: "Machine options"
icon: "linux"
comment: "AUTOGENERATED FILE, DO NOT MODIFY MANUALLY"
---

<h4 id="settings.networking.localDomain">
  <span class="hidden">`settings.networking.localDomain`</span>
</h4>

<ResponseField
    name="settings.networking.localDomain"
    type="string"
    default={"lan"}
    
    >
Local domain of the machine

Declared in [modules/networking/default.nix](https://github.com/plmercereau/nicos/blob/main/modules/networking/default.nix#L30).
</ResponseField>

<h4 id="settings.networking.localIP">
  <span class="hidden">`settings.networking.localIP`</span>
</h4>

<ResponseField
    name="settings.networking.localIP"
    type="null or string"
    
    
    >
IP of the machine in the local network

Declared in [modules/networking/default.nix](https://github.com/plmercereau/nicos/blob/main/modules/networking/default.nix#L24).
</ResponseField>

<h4 id="settings.networking.publicDomain">
  <span class="hidden">`settings.networking.publicDomain`</span>
</h4>

<ResponseField
    name="settings.networking.publicDomain"
    type="string"
    default={"public"}
    
    >
Public domain of the machine

Declared in [modules/networking/default.nix](https://github.com/plmercereau/nicos/blob/main/modules/networking/default.nix#L18).
</ResponseField>

<h4 id="settings.networking.publicIP">
  <span class="hidden">`settings.networking.publicIP`</span>
</h4>

<ResponseField
    name="settings.networking.publicIP"
    type="null or string"
    
    
    >
Public IP of the machine

Declared in [modules/networking/default.nix](https://github.com/plmercereau/nicos/blob/main/modules/networking/default.nix#L12).
</ResponseField>

<h4 id="settings.networking.vpn.bastion.enable">
  <span class="hidden">`settings.networking.vpn.bastion.enable`</span>
</h4>

<ResponseField
    name="settings.networking.vpn.bastion.enable"
    type="boolean"
    default={false}
    
    >
Whether to enable the Wireguard VPN server on this machine.


Declared in [modules/networking/vpn/bastion.nix](https://github.com/plmercereau/nicos/blob/main/modules/networking/vpn/bastion.nix#L16).
</ResponseField>

<h4 id="settings.networking.vpn.bastion.externalInterface">
  <span class="hidden">`settings.networking.vpn.bastion.externalInterface`</span>
</h4>

<ResponseField
    name="settings.networking.vpn.bastion.externalInterface"
    type="string"
    default={"eth0"}
    
    >
External interface of the bastion for NAT.


Declared in [modules/networking/vpn/bastion.nix](https://github.com/plmercereau/nicos/blob/main/modules/networking/vpn/bastion.nix#L31).
</ResponseField>

<h4 id="settings.networking.vpn.bastion.extraMachines">
  <span class="hidden">`settings.networking.vpn.bastion.extraMachines`</span>
</h4>

<ResponseField
    name="settings.networking.vpn.bastion.extraMachines"
    type="attribute set of (submodule)"
    default={{}}
    
    >
Extra machines to add to the VPN.

This is useful when you want to add a machine to the VPN that is not part of the cluster.


Declared in [modules/networking/vpn/bastion.nix](https://github.com/plmercereau/nicos/blob/main/modules/networking/vpn/bastion.nix#L38).
</ResponseField>

<h4 id="settings.networking.vpn.bastion.extraMachines.<name>.id">
  <span class="hidden">`settings.networking.vpn.bastion.extraMachines.<name>.id`</span>
</h4>

<ResponseField
    name="settings.networking.vpn.bastion.extraMachines.<name>.id"
    type="null or signed integer"
    
    
    >
Id of the machine. Each machine must have an unique value.

This id will be translated into an IP with `settings.networking.vpn.cidr` when using the VPN module.


Declared in [modules/networking/vpn/bastion.nix](https://github.com/plmercereau/nicos/blob/main/modules/networking/vpn/bastion.nix#L46).
</ResponseField>

<h4 id="settings.networking.vpn.bastion.extraMachines.<name>.publicKey">
  <span class="hidden">`settings.networking.vpn.bastion.extraMachines.<name>.publicKey`</span>
</h4>

<ResponseField
    name="settings.networking.vpn.bastion.extraMachines.<name>.publicKey"
    type="null or string"
    
    
    >
Wireguard public key of the machine.

This value is required when the VPN is enabled.


Declared in [modules/networking/vpn/bastion.nix](https://github.com/plmercereau/nicos/blob/main/modules/networking/vpn/bastion.nix#L46).
</ResponseField>

<h4 id="settings.networking.vpn.bastion.port">
  <span class="hidden">`settings.networking.vpn.bastion.port`</span>
</h4>

<ResponseField
    name="settings.networking.vpn.bastion.port"
    type="signed integer"
    default={51820}
    
    >

This port must not be block by an external firewall so clients can reach it.


Declared in [modules/networking/vpn/bastion.nix](https://github.com/plmercereau/nicos/blob/main/modules/networking/vpn/bastion.nix#L23).
</ResponseField>

<h4 id="settings.networking.vpn.cidr">
  <span class="hidden">`settings.networking.vpn.cidr`</span>
</h4>

<ResponseField
    name="settings.networking.vpn.cidr"
    type="string"
    default={"10.100.0.0/24"}
    
    >
CIDR that defines the VPN network.

It is also required to determine the machine IP address from the machine ID on the VPN.

For instance, if the CIDR is `10.100.0.0/24` and `settings.vpn.id` is `5`, then the machine IP address will be `10.100.0.5`.


Declared in [modules/networking/vpn/default.nix](https://github.com/plmercereau/nicos/blob/main/modules/networking/vpn/default.nix#L33).
</ResponseField>

<h4 id="settings.networking.vpn.domain">
  <span class="hidden">`settings.networking.vpn.domain`</span>
</h4>

<ResponseField
    name="settings.networking.vpn.domain"
    type="string"
    default={"vpn"}
    
    >
Domain name of the VPN.

The machines will then be accessible through `hostname.domain`.


Declared in [modules/networking/vpn/default.nix](https://github.com/plmercereau/nicos/blob/main/modules/networking/vpn/default.nix#L44).
</ResponseField>

<h4 id="settings.networking.vpn.enable">
  <span class="hidden">`settings.networking.vpn.enable`</span>
</h4>

<ResponseField
    name="settings.networking.vpn.enable"
    type="boolean"
    default={false}
    
    >
Whether to enable the Wireguard VPN.
```nix Example
true
```

Declared in [modules/networking/vpn/default.nix](https://github.com/plmercereau/nicos/blob/main/modules/networking/vpn/default.nix#L14).
</ResponseField>

<h4 id="settings.networking.vpn.id">
  <span class="hidden">`settings.networking.vpn.id`</span>
</h4>

<ResponseField
    name="settings.networking.vpn.id"
    type="null or signed integer"
    
    
    >
Id of the machine. Each machine must have an unique value.

This id will be translated into an IP with `settings.networking.vpn.cidr` when using the VPN module.


Declared in [modules/networking/vpn/default.nix](https://github.com/plmercereau/nicos/blob/main/modules/networking/vpn/default.nix#L15).
</ResponseField>

<h4 id="settings.networking.vpn.interface">
  <span class="hidden">`settings.networking.vpn.interface`</span>
</h4>

<ResponseField
    name="settings.networking.vpn.interface"
    type="string"
    default={"wg0"}
    
    >
Name of the interface of the VPN interface.


Declared in [modules/networking/vpn/default.nix](https://github.com/plmercereau/nicos/blob/main/modules/networking/vpn/default.nix#L53).
</ResponseField>

<h4 id="settings.networking.vpn.publicKey">
  <span class="hidden">`settings.networking.vpn.publicKey`</span>
</h4>

<ResponseField
    name="settings.networking.vpn.publicKey"
    type="null or string"
    
    
    >
Wireguard public key of the machine.

This value is required when the VPN is enabled.


Declared in [modules/networking/vpn/default.nix](https://github.com/plmercereau/nicos/blob/main/modules/networking/vpn/default.nix#L24).
</ResponseField>

<h4 id="settings.services.fs.enable">
  <span class="hidden">`settings.services.fs.enable`</span>
</h4>

<ResponseField
    name="settings.services.fs.enable"
    type="boolean"
    default={true}
    
    >
Enable services related to better filesystem management, for instance fstrim and udisks2.

Declared in [modules/services/fs/default.nix](https://github.com/plmercereau/nicos/blob/main/modules/services/fs/default.nix#L11).
</ResponseField>

<h4 id="settings.services.gitDaemon.repos">
  <span class="hidden">`settings.services.gitDaemon.repos`</span>
</h4>

<ResponseField
    name="settings.services.gitDaemon.repos"
    type="attribute set of path"
    default={{}}
    
    >
TODO

Declared in [modules/services/git-daemon.nix](https://github.com/plmercereau/nicos/blob/main/modules/services/git-daemon.nix#L13).
</ResponseField>

<h4 id="settings.services.kubernetes.enable">
  <span class="hidden">`settings.services.kubernetes.enable`</span>
</h4>

<ResponseField
    name="settings.services.kubernetes.enable"
    type="boolean"
    default={false}
    
    >
Run a k3s Kubernetes node on the machine.

Declared in [modules/services/kubernetes/default.nix](https://github.com/plmercereau/nicos/blob/main/modules/services/kubernetes/default.nix#L13).
</ResponseField>

<h4 id="settings.services.kubernetes.fleet.clustersNamespace">
  <span class="hidden">`settings.services.kubernetes.fleet.clustersNamespace`</span>
</h4>

<ResponseField
    name="settings.services.kubernetes.fleet.clustersNamespace"
    type="string"
    default={"clusters"}
    
    >
Namespace where the clusters are defined when running in upstream mode

Declared in [modules/services/kubernetes/fleet/default.nix](https://github.com/plmercereau/nicos/blob/main/modules/services/kubernetes/fleet/default.nix#L25).
</ResponseField>

<h4 id="settings.services.kubernetes.fleet.connectionUser">
  <span class="hidden">`settings.services.kubernetes.fleet.connectionUser`</span>
</h4>

<ResponseField
    name="settings.services.kubernetes.fleet.connectionUser"
    type="string"
    default={"fleet-connection-user"}
    
    >
User to connect to the upstream machine and patch the kubeconfig secret of the downstream cluster

Declared in [modules/services/kubernetes/fleet/default.nix](https://github.com/plmercereau/nicos/blob/main/modules/services/kubernetes/fleet/default.nix#L40).
</ResponseField>

<h4 id="settings.services.kubernetes.fleet.enable">
  <span class="hidden">`settings.services.kubernetes.fleet.enable`</span>
</h4>

<ResponseField
    name="settings.services.kubernetes.fleet.enable"
    type="boolean"
    default={false}
    
    >
Enable fleet

Declared in [modules/services/kubernetes/fleet/default.nix](https://github.com/plmercereau/nicos/blob/main/modules/services/kubernetes/fleet/default.nix#L15).
</ResponseField>

<h4 id="settings.services.kubernetes.fleet.fleetNamespace">
  <span class="hidden">`settings.services.kubernetes.fleet.fleetNamespace`</span>
</h4>

<ResponseField
    name="settings.services.kubernetes.fleet.fleetNamespace"
    type="string"
    default={"fleet-system"}
    
    >
Namespace where fleet will run

Declared in [modules/services/kubernetes/fleet/default.nix](https://github.com/plmercereau/nicos/blob/main/modules/services/kubernetes/fleet/default.nix#L20).
</ResponseField>

<h4 id="settings.services.kubernetes.fleet.gitRepos">
  <span class="hidden">`settings.services.kubernetes.fleet.gitRepos`</span>
</h4>

<ResponseField
    name="settings.services.kubernetes.fleet.gitRepos"
    type="attribute set of (submodule)"
    default={{}}
    
    >
List of Git repositories to add to Fleet as GitRepo resources.

Each resource will be built as a derivation, then symlinked as a K3s addon manifest on system activation.


Declared in [modules/services/kubernetes/fleet/manager.nix](https://github.com/plmercereau/nicos/blob/main/modules/services/kubernetes/fleet/manager.nix#L54).
</ResponseField>

<h4 id="settings.services.kubernetes.fleet.gitRepos.<name>.branch">
  <span class="hidden">`settings.services.kubernetes.fleet.gitRepos.<name>.branch`</span>
</h4>

<ResponseField
    name="settings.services.kubernetes.fleet.gitRepos.<name>.branch"
    type="string"
    default={"main"}
    
    >
branch of the git repository

Declared in [modules/services/kubernetes/fleet/manager.nix](https://github.com/plmercereau/nicos/blob/main/modules/services/kubernetes/fleet/manager.nix#L20).
</ResponseField>

<h4 id="settings.services.kubernetes.fleet.gitRepos.<name>.namespace">
  <span class="hidden">`settings.services.kubernetes.fleet.gitRepos.<name>.namespace`</span>
</h4>

<ResponseField
    name="settings.services.kubernetes.fleet.gitRepos.<name>.namespace"
    type="string"
    default={"default"}
    
    >
namespace of the git repository

Declared in [modules/services/kubernetes/fleet/manager.nix](https://github.com/plmercereau/nicos/blob/main/modules/services/kubernetes/fleet/manager.nix#L15).
</ResponseField>

<h4 id="settings.services.kubernetes.fleet.gitRepos.<name>.paths">
  <span class="hidden">`settings.services.kubernetes.fleet.gitRepos.<name>.paths`</span>
</h4>

<ResponseField
    name="settings.services.kubernetes.fleet.gitRepos.<name>.paths"
    type="list of string"
    default={["*"]}
    
    >
paths to be included in the git repository

Declared in [modules/services/kubernetes/fleet/manager.nix](https://github.com/plmercereau/nicos/blob/main/modules/services/kubernetes/fleet/manager.nix#L25).
</ResponseField>

<h4 id="settings.services.kubernetes.fleet.gitRepos.<name>.repo">
  <span class="hidden">`settings.services.kubernetes.fleet.gitRepos.<name>.repo`</span>
</h4>

<ResponseField
    name="settings.services.kubernetes.fleet.gitRepos.<name>.repo"
    type="string"
    
    required
    >
URL of the git repository

Declared in [modules/services/kubernetes/fleet/manager.nix](https://github.com/plmercereau/nicos/blob/main/modules/services/kubernetes/fleet/manager.nix#L59).
</ResponseField>

<h4 id="settings.services.kubernetes.fleet.gitRepos.<name>.targets">
  <span class="hidden">`settings.services.kubernetes.fleet.gitRepos.<name>.targets`</span>
</h4>

<ResponseField
    name="settings.services.kubernetes.fleet.gitRepos.<name>.targets"
    type="list of (attribute set)"
    default={[]}
    
    >
Target clusters to deploy the GitRepo to. If none is specified, nothing will be deployed.

Declared in [modules/services/kubernetes/fleet/manager.nix](https://github.com/plmercereau/nicos/blob/main/modules/services/kubernetes/fleet/manager.nix#L30).
</ResponseField>

<h4 id="settings.services.kubernetes.fleet.helmChartVersion">
  <span class="hidden">`settings.services.kubernetes.fleet.helmChartVersion`</span>
</h4>

<ResponseField
    name="settings.services.kubernetes.fleet.helmChartVersion"
    type="string"
    default={"0.9.0"}
    
    >
Fleet Helm chart version

Declared in [modules/services/kubernetes/fleet/default.nix](https://github.com/plmercereau/nicos/blob/main/modules/services/kubernetes/fleet/default.nix#L55).
</ResponseField>

<h4 id="settings.services.kubernetes.fleet.labels">
  <span class="hidden">`settings.services.kubernetes.fleet.labels`</span>
</h4>

<ResponseField
    name="settings.services.kubernetes.fleet.labels"
    type="attribute set of string"
    default={{}}
    
    >
Labels to add to the cluster when running in multi-cluster mode

Declared in [modules/services/kubernetes/fleet/default.nix](https://github.com/plmercereau/nicos/blob/main/modules/services/kubernetes/fleet/default.nix#L45).
</ResponseField>

<h4 id="settings.services.kubernetes.fleet.localGitRepos">
  <span class="hidden">`settings.services.kubernetes.fleet.localGitRepos`</span>
</h4>

<ResponseField
    name="settings.services.kubernetes.fleet.localGitRepos"
    type="attribute set of (submodule)"
    default={{}}
    
    >
Set of local paths to be used as git repositories for Fleet.

For each entry, a GitRepo will be created and will point to a repo served by a git daemon running on the host machine.

Declared in [modules/services/kubernetes/fleet/manager.nix](https://github.com/plmercereau/nicos/blob/main/modules/services/kubernetes/fleet/manager.nix#L37).
</ResponseField>

<h4 id="settings.services.kubernetes.fleet.localGitRepos.<name>.branch">
  <span class="hidden">`settings.services.kubernetes.fleet.localGitRepos.<name>.branch`</span>
</h4>

<ResponseField
    name="settings.services.kubernetes.fleet.localGitRepos.<name>.branch"
    type="string"
    default={"main"}
    
    >
branch of the git repository

Declared in [modules/services/kubernetes/fleet/manager.nix](https://github.com/plmercereau/nicos/blob/main/modules/services/kubernetes/fleet/manager.nix#L20).
</ResponseField>

<h4 id="settings.services.kubernetes.fleet.localGitRepos.<name>.namespace">
  <span class="hidden">`settings.services.kubernetes.fleet.localGitRepos.<name>.namespace`</span>
</h4>

<ResponseField
    name="settings.services.kubernetes.fleet.localGitRepos.<name>.namespace"
    type="string"
    default={"default"}
    
    >
namespace of the git repository

Declared in [modules/services/kubernetes/fleet/manager.nix](https://github.com/plmercereau/nicos/blob/main/modules/services/kubernetes/fleet/manager.nix#L15).
</ResponseField>

<h4 id="settings.services.kubernetes.fleet.localGitRepos.<name>.package">
  <span class="hidden">`settings.services.kubernetes.fleet.localGitRepos.<name>.package`</span>
</h4>

<ResponseField
    name="settings.services.kubernetes.fleet.localGitRepos.<name>.package"
    type="path"
    
    required
    >
Local path that will be used as the git repository

Declared in [modules/services/kubernetes/fleet/manager.nix](https://github.com/plmercereau/nicos/blob/main/modules/services/kubernetes/fleet/manager.nix#L42).
</ResponseField>

<h4 id="settings.services.kubernetes.fleet.localGitRepos.<name>.paths">
  <span class="hidden">`settings.services.kubernetes.fleet.localGitRepos.<name>.paths`</span>
</h4>

<ResponseField
    name="settings.services.kubernetes.fleet.localGitRepos.<name>.paths"
    type="list of string"
    default={["*"]}
    
    >
paths to be included in the git repository

Declared in [modules/services/kubernetes/fleet/manager.nix](https://github.com/plmercereau/nicos/blob/main/modules/services/kubernetes/fleet/manager.nix#L25).
</ResponseField>

<h4 id="settings.services.kubernetes.fleet.localGitRepos.<name>.targets">
  <span class="hidden">`settings.services.kubernetes.fleet.localGitRepos.<name>.targets`</span>
</h4>

<ResponseField
    name="settings.services.kubernetes.fleet.localGitRepos.<name>.targets"
    type="list of (attribute set)"
    default={[]}
    
    >
Target clusters to deploy the GitRepo to. If none is specified, nothing will be deployed.

Declared in [modules/services/kubernetes/fleet/manager.nix](https://github.com/plmercereau/nicos/blob/main/modules/services/kubernetes/fleet/manager.nix#L30).
</ResponseField>

<h4 id="settings.services.kubernetes.fleet.mode">
  <span class="hidden">`settings.services.kubernetes.fleet.mode`</span>
</h4>

<ResponseField
    name="settings.services.kubernetes.fleet.mode"
    type="one of &#34;standalone&#34;, &#34;upstream&#34;, &#34;downstream&#34;"
    default={"standalone"}
    
    >
Fleet mode.
Standalone will install the manager and agent on the same node, and will manage its own applications.
Upstream will install the manager, and will manage applications on downstream clusters. There should be only one upstream cluster in the project.
Downstream will install the agent, and will manage applications on the upstream cluster. An upstream cluster is required in the project.


Declared in [modules/services/kubernetes/fleet/default.nix](https://github.com/plmercereau/nicos/blob/main/modules/services/kubernetes/fleet/default.nix#L30).
</ResponseField>

<h4 id="settings.services.kubernetes.fleet.values">
  <span class="hidden">`settings.services.kubernetes.fleet.values`</span>
</h4>

<ResponseField
    name="settings.services.kubernetes.fleet.values"
    type="attribute set of string"
    default={{}}
    
    >
Template values of the cluster

Declared in [modules/services/kubernetes/fleet/default.nix](https://github.com/plmercereau/nicos/blob/main/modules/services/kubernetes/fleet/default.nix#L50).
</ResponseField>

<h4 id="settings.services.kubernetes.group">
  <span class="hidden">`settings.services.kubernetes.group`</span>
</h4>

<ResponseField
    name="settings.services.kubernetes.group"
    type="string"
    default={"k8s-admin"}
    
    >
Group that has access to the k3s config and data.

Declared in [modules/services/kubernetes/default.nix](https://github.com/plmercereau/nicos/blob/main/modules/services/kubernetes/default.nix#L18).
</ResponseField>

<h4 id="settings.services.mdns.enable">
  <span class="hidden">`settings.services.mdns.enable`</span>
</h4>

<ResponseField
    name="settings.services.mdns.enable"
    type="boolean"
    default={true}
    
    >
Enable mDNS service (avahi).

Declared in [modules/networking/mdns/default.nix](https://github.com/plmercereau/nicos/blob/main/modules/networking/mdns/default.nix#L11).
</ResponseField>

<h4 id="settings.services.nix-builder.enable">
  <span class="hidden">`settings.services.nix-builder.enable`</span>
</h4>

<ResponseField
    name="settings.services.nix-builder.enable"
    type="boolean"
    default={false}
    
    >
Whether to enable the machine as a Nix builder for the other machines.
```nix Example
true
```

Declared in [modules/services/nix-builder/default.nix](https://github.com/plmercereau/nicos/blob/main/modules/services/nix-builder/default.nix#L18).
</ResponseField>

<h4 id="settings.services.nix-builder.maxJobs">
  <span class="hidden">`settings.services.nix-builder.maxJobs`</span>
</h4>

<ResponseField
    name="settings.services.nix-builder.maxJobs"
    type="signed integer"
    default={1}
    
    >
The maximum number of jobs that can be run in parallel on the builder.
The default is _nix.settings.cores_ if it is greater than 0, otherwise 1


Declared in [modules/services/nix-builder/default.nix](https://github.com/plmercereau/nicos/blob/main/modules/services/nix-builder/default.nix#L52).
</ResponseField>

<h4 id="settings.services.nix-builder.speedFactor">
  <span class="hidden">`settings.services.nix-builder.speedFactor`</span>
</h4>

<ResponseField
    name="settings.services.nix-builder.speedFactor"
    type="signed integer"
    default={1}
    
    >
The speed factor of the builder. The speed factor is used to
prioritize builders when multiple builders are available.
The higher the speed factor, the more likely it is that the builder
will be used.


Declared in [modules/services/nix-builder/default.nix](https://github.com/plmercereau/nicos/blob/main/modules/services/nix-builder/default.nix#L42).
</ResponseField>

<h4 id="settings.services.nix-builder.ssh.privateKeyFile">
  <span class="hidden">`settings.services.nix-builder.ssh.privateKeyFile`</span>
</h4>

<ResponseField
    name="settings.services.nix-builder.ssh.privateKeyFile"
    type="null or string"
    
    
    >
The private key file of the Nix builder.

Declared in [modules/services/nix-builder/default.nix](https://github.com/plmercereau/nicos/blob/main/modules/services/nix-builder/default.nix#L24).
</ResponseField>

<h4 id="settings.services.nix-builder.ssh.publicKey">
  <span class="hidden">`settings.services.nix-builder.ssh.publicKey`</span>
</h4>

<ResponseField
    name="settings.services.nix-builder.ssh.publicKey"
    type="null or string"
    
    
    >
The public key of the Nix builder.

Declared in [modules/services/nix-builder/default.nix](https://github.com/plmercereau/nicos/blob/main/modules/services/nix-builder/default.nix#L29).
</ResponseField>

<h4 id="settings.services.nix-builder.ssh.user">
  <span class="hidden">`settings.services.nix-builder.ssh.user`</span>
</h4>

<ResponseField
    name="settings.services.nix-builder.ssh.user"
    type="string"
    default={"builder"}
    
    >
The user name of the Nix builder.

Declared in [modules/services/nix-builder/default.nix](https://github.com/plmercereau/nicos/blob/main/modules/services/nix-builder/default.nix#L19).
</ResponseField>

<h4 id="settings.services.nix-builder.supportedFeatures">
  <span class="hidden">`settings.services.nix-builder.supportedFeatures`</span>
</h4>

<ResponseField
    name="settings.services.nix-builder.supportedFeatures"
    type="list of string"
    default={["nixos-test","benchmark","big-parallel","kvm"]}
    
    >
A list of features that the builder supports


Declared in [modules/services/nix-builder/default.nix](https://github.com/plmercereau/nicos/blob/main/modules/services/nix-builder/default.nix#L35).
</ResponseField>

<h4 id="settings.services.time.enable">
  <span class="hidden">`settings.services.time.enable`</span>
</h4>

<ResponseField
    name="settings.services.time.enable"
    type="boolean"
    default={true}
    
    >
Enable timesyncd and htpdate.

Declared in [modules/services/time/default.nix](https://github.com/plmercereau/nicos/blob/main/modules/services/time/default.nix#L11).
</ResponseField>

<h4 id="settings.ssh.fail2ban.enable">
  <span class="hidden">`settings.ssh.fail2ban.enable`</span>
</h4>

<ResponseField
    name="settings.ssh.fail2ban.enable"
    type="boolean"
    default={false}
    
    >
Enable fail2ban to block SSH brute force attacks.

By default, Fail2ban is enabled if sshguard is disabled.


Declared in [modules/networking/ssh/default.nix](https://github.com/plmercereau/nicos/blob/main/modules/networking/ssh/default.nix#L22).
</ResponseField>

<h4 id="settings.ssh.sshguard.enable">
  <span class="hidden">`settings.ssh.sshguard.enable`</span>
</h4>

<ResponseField
    name="settings.ssh.sshguard.enable"
    type="boolean"
    default={true}
    
    >
Enable sshguard to block SSH brute force attacks.

Declared in [modules/networking/ssh/default.nix](https://github.com/plmercereau/nicos/blob/main/modules/networking/ssh/default.nix#L31).
</ResponseField>

<h4 id="settings.sshPublicKey">
  <span class="hidden">`settings.sshPublicKey`</span>
</h4>

<ResponseField
    name="settings.sshPublicKey"
    type="string"
    
    required
    >
SSH public key of the machine.
    
This option is required to decode the secrets defined in the main features like users, wireless networks, vpn, etc.

Declared in [modules/networking/ssh/default.nix](https://github.com/plmercereau/nicos/blob/main/modules/networking/ssh/default.nix#L12).
</ResponseField>

<h4 id="settings.system.impermanence.enable">
  <span class="hidden">`settings.system.impermanence.enable`</span>
</h4>

<ResponseField
    name="settings.system.impermanence.enable"
    type="boolean"
    default={false}
    
    >
Whether to enable impermanence.
```nix Example
true
```

Declared in [modules/impermanence/default.nix](https://github.com/plmercereau/nicos/blob/main/modules/impermanence/default.nix#L11).
</ResponseField>

<h4 id="settings.system.impermanence.persistentSystemPath">
  <span class="hidden">`settings.system.impermanence.persistentSystemPath`</span>
</h4>

<ResponseField
    name="settings.system.impermanence.persistentSystemPath"
    type="string"
    default={"/nix/persist/system"}
    
    >
Path to where the persisted part of the system lies

Declared in [modules/impermanence/default.nix](https://github.com/plmercereau/nicos/blob/main/modules/impermanence/default.nix#L12).
</ResponseField>

<h4 id="settings.system.swap.file.enable">
  <span class="hidden">`settings.system.swap.file.enable`</span>
</h4>

<ResponseField
    name="settings.system.swap.file.enable"
    type="boolean"
    default={true}
    
    >
Enable a swap file on the root partition.

Declared in [modules/swap/default.nix](https://github.com/plmercereau/nicos/blob/main/modules/swap/default.nix#L19).
</ResponseField>

<h4 id="settings.system.swap.file.size">
  <span class="hidden">`settings.system.swap.file.size`</span>
</h4>

<ResponseField
    name="settings.system.swap.file.size"
    type="integer between 0 and 10 (both inclusive)"
    default={1}
    
    >
Size of the swap file in GiB.

Declared in [modules/swap/default.nix](https://github.com/plmercereau/nicos/blob/main/modules/swap/default.nix#L25).
</ResponseField>

<h4 id="settings.system.swap.zram.enable">
  <span class="hidden">`settings.system.swap.zram.enable`</span>
</h4>

<ResponseField
    name="settings.system.swap.zram.enable"
    type="boolean"
    default={true}
    
    >
Enable a swap file in a zram device.

Declared in [modules/swap/default.nix](https://github.com/plmercereau/nicos/blob/main/modules/swap/default.nix#L12).
</ResponseField>

<h4 id="settings.users.users">
  <span class="hidden">`settings.users.users`</span>
</h4>

<ResponseField
    name="settings.users.users"
    type="attribute set of (submodule)"
    default={{}}
    
    >
Set of users to create and configure.

Declared in [modules/users/default.nix](https://github.com/plmercereau/nicos/blob/main/modules/users/default.nix#L50).
</ResponseField>

<h4 id="settings.users.users.<name>.enable">
  <span class="hidden">`settings.users.users.<name>.enable`</span>
</h4>

<ResponseField
    name="settings.users.users.<name>.enable"
    type="boolean"
    default={false}
    
    >
Whether the user is enabled in the machine.

Declared in [modules/users/default.nix](https://github.com/plmercereau/nicos/blob/main/modules/users/default.nix#L22).
</ResponseField>

<h4 id="settings.users.users.<name>.isAdmin">
  <span class="hidden">`settings.users.users.<name>.isAdmin`</span>
</h4>

<ResponseField
    name="settings.users.users.<name>.isAdmin"
    type="boolean"
    default={false}
    
    >
Whether the user is an admin of the machine.

Declared in [modules/users/default.nix](https://github.com/plmercereau/nicos/blob/main/modules/users/default.nix#L28).
</ResponseField>

<h4 id="settings.users.users.<name>.isSystemUser">
  <span class="hidden">`settings.users.users.<name>.isSystemUser`</span>
</h4>

<ResponseField
    name="settings.users.users.<name>.isSystemUser"
    type="boolean"
    default={false}
    
    >
Whether the user is a system user.

Declared in [modules/users/default.nix](https://github.com/plmercereau/nicos/blob/main/modules/users/default.nix#L34).
</ResponseField>

<h4 id="settings.users.users.<name>.publicKeys">
  <span class="hidden">`settings.users.users.<name>.publicKeys`</span>
</h4>

<ResponseField
    name="settings.users.users.<name>.publicKeys"
    type="list of valid ecdsa-sha2-nistp256 or ssh-ed25519 or ssh-rsa key, meaning a string matching the pattern ^ecdsa-sha2-nistp256 AAAAE2VjZHNhLXNoYTItbmlzdHAyNTY[[:lower:][:upper:][:digit:]\/+]{108}=$|^ssh-ed25519 AAAAC3NzaC1lZDI1NTE5[[:lower:][:upper:][:digit:]\/+]{48}$|^ssh-rsa AAAAB3NzaC1yc2E[[:lower:][:upper:][:digit:]\/+]{355,}={0,2}$"
    default={[]}
    
    >
Public keys of the user, without the comment (user@host) part.

Declared in [modules/users/default.nix](https://github.com/plmercereau/nicos/blob/main/modules/users/default.nix#L40).
</ResponseField>

