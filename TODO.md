# do NOT deploy anything on fleet-local - of make something totally separate

OR: find out why "${ .ClusterValues }" is not interpolated

deploy-rs: is now supposed to work with sudo. Adapt accordingly
